# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Railway (for Openclaw hosting)
# Get token from: https://railway.app/account/tokens
RAILWAY_API_TOKEN=your-railway-api-token
# (Optional) Pre-built Docker image for Openclaw. If omitted, uses node:22 (Debian)
# and installs openclaw at container startup (slower but requires no pre-build step).
# Build with: docker build -f docker/Dockerfile -t your-user/palclaw-openclaw:latest . && docker push
# RAILWAY_OPENCLAW_IMAGE=your-user/palclaw-openclaw:latest

# AI Provider API Keys (injected into each Railway deployment)
# Only the key matching the user's selected model is used per deploy.
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GEMINI_API_KEY=AI...

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000
